/**
 * Client-side hooks — index
 *
 * All React hooks that require client-side execution.
 *
 * Convention:
 *  - File names: use-<hook-name>.ts
 *  - All hook files must be used only in "use client" components
 *  - Server-side data fetching happens in Server Components — not hooks
 *
 * Planned hooks:
 *  useCart()           — cart state (client-side reactive cart count)
 *  useMediaQuery()     — responsive breakpoint detection
 *  useDebounce()       — debounce input values
 *  useLocalStorage()   — local storage with SSR safety
 *
 * Phase: Not yet implemented — stubs for architecture.
 */

export {};
